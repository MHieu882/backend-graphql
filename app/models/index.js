const User = require('./user');
const Comment = require('./comment');
const Clap = require('./clap');
const Post = require('./post');
const SubComment = require('./subcoment');
const follow = require('./follow');

module.exports = {
  User,
  Comment,
  Clap,
  Post,
  SubComment,
  follow,
};
